import { Alliance, Gender, Status } from "./interfaces";

export const GenderFilters = [Gender.ALL, Gender.MALE, Gender.FEMALE];
export const StatusFilters = [Status.ALL, Status.ALIVE, Status.DECEASED];
export const AllianceFilters = [
  Alliance.ALL,
  Alliance.MARLEY,
  Alliance.ELDIA,
  Alliance.NONE,
];
